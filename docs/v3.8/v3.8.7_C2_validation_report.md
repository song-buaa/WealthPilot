# v3.8.7 C2 验收报告

## 0. C2 三件事

1. 修 user_query 透传 bug（C2_carryover.md (a)）
2. 接通 wp-retrieve-principles 双轨+flag（general 路由）
3. 重建 general 真实基线（C2_carryover.md (b)）

## 1. user_query bug 修复前后对比

| | 修复前（v3.8.6） | 修复后 |
|---|---|---|
| 查询 | "再平衡的纪律是什么" | 同 |
| invoked_skills | `[]` | `["wp-retrieve-principles"]` |
| skill_results | `{skipped: True, reason: non_investment_query}` | `{chunks_count: 3, triggered_by: investment_keyword_match}` |
| retrieved_principles | 0 条 | 3 条 |
| 对账 is_consistent | False | **True** |
| 非投资查询（"今天天气"） | skipped（不变） | skipped（不变） |

## 2. 双轨等价证据

### 直接调用对比（ExecutingAgent.run）

| | Flag OFF（直连） | Flag ON（invoke_skill + 适配） |
|---|---|---|
| invoked_skills | `["wp-retrieve-principles"]` | `["wp-retrieve-principles"]` |
| count | 3 | 3 |
| type(rp[0]) | `RetrievedChunk` | `RetrievedChunk` |
| content[:60] | `## 区间与纪律的联动\nWealthPilot 的目标区间应与...` | 完全相同 |
| source_type | `allocation_principles` | `allocation_principles` |

### SSE 真实回答对比

| | Flag OFF | Flag ON |
|---|---|---|
| 回答长度 | 609 字 | 632 字 |
| 内容 | 正常带原则知识（目标区间、纪律联动、分级偏离） | 正常带原则知识（区间联动、偏离度告警、逆向操作） |
| dict 泄漏 | **无** | **无** |

内容因 LLM 非确定性略有不同，但都正确引用了原则知识、无 dict 打印泄漏。

### 返回契约适配

`_adapt_retrieve_result(raw)` 把 `invoke_skill` 返回的 `dict{"chunks":[dict...]}` 转回
`list[RetrievedChunk]`（用 `RetrievedChunk.model_validate`），保证 flag on/off 两条路
塞进 `LoadedData.retrieved_principles` 的是同类型对象，下游 `getattr(c, "content")` 正常工作。

## 3. 重建 general 真实基线

### Case5_general_invest（修 bug 后真实基线）

| 字段 | v3.8.1/v3.8.2 假基线（单测） | C2 真实基线 | 说明 |
|------|---------------------------|-----------|------|
| route | general | general | |
| declared_exec | ["wp-retrieve-principles"] | ["wp-retrieve-principles"] | |
| invoked_exec | ["wp-retrieve-principles"] | ["wp-retrieve-principles"] | 假基线凑巧对了，但来源不同 |
| matched | ["wp-retrieve-principles"] | ["wp-retrieve-principles"] | |
| declared_not_invoked | [] | [] | |
| invoked_not_declared | [] | [] | |
| is_consistent | True | **True** | |
| has_unknown | False | False | |

### Case5b_general_invest

同 Case5，`is_consistent=True`、`matched=["wp-retrieve-principles"]`。

**此为修 user_query bug 后的真实基线，取代 v3.8.1/v3.8.2 中来自单测的假 Case5。**

说明：v3.8.1/v3.8.2 假基线的值碰巧和真实基线一致（都是 `is_consistent=True`），
但来源完全不同——假基线是单测硬编码 `invoked=["wp-retrieve-principles"]`，
真实基线是修 bug 后 `_execute_general` 确实收到了 user_query、关键词匹配命中、
KnowledgeStore 真实检索返回 3 条结果。

### Case6_general_noninvest

非投资话题走 `low_confidence` 路由，不进 `_execute_general`，不产出对账。
此类场景无需基线（ReviewingAgent 也不触发）。

## 4. position/portfolio 对账不变（Case1-4）

| Case | 字段 | C0/C1 基线 | C2 实际 | 一致? |
|------|------|----------|--------|-------|
| Case1 pos_main | matched | ["wp-check-discipline","wp-generate-signals"] | 同 | ✅ |
| | dni | ["wp-fetch-holdings","wp-fetch-research","wp-retrieve-principles"] | 同 | ✅ |
| | ind | ["wp-load-context"] | 同 | ✅ |
| | ic/hu | F/F | F/F | ✅ |
| Case1b pos_main | 同 Case1 | 同 | 同 | ✅ |
| Case2 new_entry | matched=[], dni=全5项, pseudo=2项 | 同 | 同 | ✅ |
| Case3 pf_review | matched=["wp-fetch-research"] | 同 | 同 | ✅ |
| Case3b pf_review | 同 Case3 | 同 | 同 | ✅ |
| Case4 pf_perf | matched=[], dni=3项 | 同 | 同 | ✅ |
| Case4b pf_alloc | 同 Case4 | 同 | 同 | ✅ |

**Case1-4 全等。C2 没碰 data_loader.load()，position/portfolio 可信基线不受影响。**

## 5. M5 回归

**18/18 全绿**（flag OFF）。

## 6. 前端零感知

```bash
git diff HEAD -- backend/agents/contracts.py backend/services/decision_service_v3.py backend/api/ frontend/src/
```
→ 空（零改动）。C2 只改了 `backend/agents/executing_agent.py`。

## 7. flag 默认关确认

```
_use_skill_retrieve_principles() = False
_use_skill_output_validator() = False
✅ 两个 flag 默认都关
```

## 8. 综合判定

| 标准 | 结果 |
|------|------|
| user_query bug 修复 + 前后对比 | ✅ |
| 双轨等价（直调 + SSE，无 dict 泄漏） | ✅ |
| general 真实基线重建（is_consistent=True） | ✅ |
| Case1-4 对账不变 | ✅ |
| M5 18/18 | ✅ |
| 前端零感知 | ✅ |
| 两个 flag 默认关 | ✅ |

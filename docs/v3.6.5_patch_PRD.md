# v3.6.5 Patch PRD: wp-retrieve-principles 半接通修复

**类型**: 小补丁
**前置**: v3.7.0 tag 已打
**目标**: 修复 v3.6 引入 wp-retrieve-principles 时未完成的"LLM 注入半接通"问题

## Step 1: M5 脚本修复 + 基线建立

- `scripts/m5_e2e_18_cases.py`: `session_id` -> `conversation_id` (3 处)
- `scripts/m5_e2e_18_cases.py`: `timeout=60` -> `timeout=180` (VPN 环境适配)
- 基线: `docs/m5_e2e_report.v3.7_baseline.md` (18/18 通过)

## Step 1.5: portfolio 路由 explain API 序列化补齐

**发现时机**: 用户做基线截图时发现 portfolio 路由的 ExplainPanel 不显示知识库引用。

**根因**: `decision_service_v3.py:627-632` 手工构造的 `explain_data["data"]` dict 漏掉了 `retrieved_principles` 和 `retrieved_research_views` 字段。position 路由走 `_serialize_decision_result()` 有这些字段，portfolio 路由没有。

**改动**: `backend/services/decision_service_v3.py` 第 627-632 行的 `"data"` dict 追加两个字段，序列化逻辑复用 `decision_service.py:746-767` 的现有模式。

**决策追溯**: 本质同属 v3.6 半接通问题，分开修会导致 portfolio 路由验收基线不可靠，故纳入 v3.6.5。

## Step 2: reason() 注入 retrieved_principles

- 新增 `_PRINCIPLES_PROMPT_TEMPLATE` 常量
- `reason()` 内部提取 `data.retrieved_principles` 并格式化为 prompt 段落
- 拼接到 system prompt: `_BASE_PROMPT + context_prompt + principles_prompt + position_prompt`
- 删除 v3.7 加的"半接通"日志 (executing_agent.py, 2 处)

## Step 3: 删除 _build_citation_block 死代码

- `expressing_agent.py`: 删除 `_build_citation_block` 函数定义 + `_SOURCE_TYPE_LABELS` 常量
- v3.6.1 已用前端 KnowledgeCitations 组件替代

## 验收

### 4.1 自动验证
- M5 18/18 通过
- pytest 全绿
- grep 死代码/半接通日志为空

### 4.2 前端验收 (用户手工)
- 5 个用例对比基线截图
- 决策档位稳定 + 推理明确引用用户原则

# C1/C2 Flag 翻开验证记录

## 改动

两个 flag 默认值从"关"翻成"开"：
- `_use_skill_output_validator()`: `os.environ.get(...,"1") != "0"`（默认 True）
- `_use_skill_retrieve_principles()`: `os.environ.get(...,"1") != "0"`（默认 True）

显式设 `=0` 可切回老直连（双轨保留）。

## 验证结果

### 1. 默认走 Skill 机制（invoke_skill 日志）

不设任何环境变量，后端日志出现：
```
[SKILL调用] invoke_skill: wp-output-validator      ← position 请求触发
[SKILL调用] invoke_skill: wp-retrieve-principles    ← general 投资请求触发
```

### 2. M5 回归：18/18（flag 默认开）

### 3. 对账
- position: `matched=["wp-check-discipline","wp-generate-signals"]`，与基线一致
- general invest: `matched=["wp-retrieve-principles"]`, `is_consistent=True`

### 4. 前端零感知：contracts/decision_service_v3/api/frontend 零改动

### 5. 反向确认：`=0` 切回老直连
```
validator (=0): False
retrieve  (=0): False
```

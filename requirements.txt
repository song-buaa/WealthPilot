# ── 原有依赖（保留，Streamlit 版本仍可运行）────────────────────────────────
streamlit>=1.32.0
sqlalchemy>=2.0.0
pandas>=2.0.0
openai>=1.20.0
anthropic>=0.86.0
httpx[socks]>=0.24.0
plotly>=5.20.0

# ── Phase 1 新增：FastAPI 后端 ───────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # FastAPI 文件上传必需
python-dotenv>=1.0.0     # 从 .env 文件加载环境变量

# ── 投研观点模块可选依赖 ─────────────────────────────────────────────────────
pypdf>=4.0.0            # PDF 文字提取
requests>=2.31.0        # URL 抓取
beautifulsoup4>=4.12.0  # HTML 解析
